# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

vars:
  HUGO: hugo

tasks:
  default:
    desc: List all available tasks.
    cmds:
      - task --list

  setup:
    desc: Set up the local dev environment (installs the pre-commit git hook).
    cmds:
      - pre-commit install

  hugo:serve:
    aliases: [serve, dev, run]
    cmd: "{{.HUGO}} serve -D"
