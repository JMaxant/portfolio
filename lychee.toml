# Shared lychee config. The internal (per-PR) check adds --offline on the CLI;
# the weekly external check runs online with these settings as-is.
no_progress = true
include_fragments = true
max_retries = 2
timeout = 20
cache = false
accept = ["200..=204", "429"]
exclude = ["^mailto:"]
